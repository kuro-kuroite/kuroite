import { UserConfig } from '@commitlint/types';

// FYI: https://commitlint.js.org/#/reference-configuration
const config: UserConfig = { extends: ['@commitlint/config-conventional'] };

export default config;
